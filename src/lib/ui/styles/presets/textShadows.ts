import {colors} from './colors'

export default {
  sm: `0 0 4px rgba(${colors.blackRGB}, 0.5);`,
  md: `1px 1px 4px rgba(${colors.blackRGB}, 0.5);`,
  lg: `0 2px 10px rgba(${colors.blackRGB}, 0.3)`,
  none: 'none',
}
