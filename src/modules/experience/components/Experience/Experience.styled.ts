import styled from 'styled-components'

import {SnapScrollContainer, SnapScrollSection} from 'modules/common/components'
import {Education, Internships, Inverse, Treehouse} from './components'

export const StyledRoot = styled(SnapScrollContainer)``

export const StyledSection = styled(SnapScrollSection)``

export const StyledInverse = styled(Inverse)``

export const StyledTreehouse = styled(Treehouse)``

export const StyledInternships = styled(Internships)``

export const StyledEducation = styled(Education)``
